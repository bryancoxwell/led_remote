[package]
name = "led_remote"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
num-complex = "0.4"
serde_json = "1"
soapysdr = { version = "0.4", optional = true }

[features]
default = []
transmit = ["dep:soapysdr"]
